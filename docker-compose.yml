version: '3.8'
services:
  postgres-db:
    image: postgres:15-alpine
    container_name: student-postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: